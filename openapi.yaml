openapi: 3.1.0
info:
  title: raphael-ops
  version: 0.1.0
  description: Observability, reliability, release operations
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
